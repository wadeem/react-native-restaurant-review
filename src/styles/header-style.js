import {StyleSheet} from 'react-native';

const headerStyle = StyleSheet.create({
    header: {
        padding: 40,
        fontSize: 30,
        color: '#0066cc',
        fontWeight: '300',
        textAlign: 'center',
    },
});

export default headerStyle;
